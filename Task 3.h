#pragma once;

int kol_prob (char* str, int size_str); // Лучше назвать getSpaceCount
int doubleprob (char* str); // // Лучше назвать getDoubleSpaceCount
void del_doubleprob (char* str1, char* str2); // Лучше назвать deleteDoubleSpaces
int kol_slov (char* str, int probeli, int size); // Лучше назвать getWordCount

int task3(); // можно возвращать void